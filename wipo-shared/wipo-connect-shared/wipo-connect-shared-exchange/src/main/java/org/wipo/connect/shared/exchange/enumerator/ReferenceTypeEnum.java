/*
 * Copyright (C) 2015 World Intellectual Property Organization (WIPO).
 * All Rights Reserved.
 *
 * This file is part of WIPO Connect.
 *
 *
 * @author Fincons
 *
 */

package org.wipo.connect.shared.exchange.enumerator;

/**
 * The Enum ReferenceTypeEnum.
 */
public enum ReferenceTypeEnum {

    /** The license type. */
    LICENSE_TYPE,

    /** The tariff type. */
    TARIFF_TYPE,

    /** The frequency. */
    FREQUENCY,

    /** The territory type. */
    TERRITORY_TYPE,

    /** The title type. */
    TITLE_TYPE,

    /** The work type. */
    WORK_TYPE,

    /** The company type. */
    COMPANY_TYPE,

    /** The catalogue type. */
    CATALOGUE_TYPE,

    /** The distribution class. */
    DISTRIBUTION_CLASS,

    /** The work attachment. */
    WORK_ATTACHMENT,

    /** The work source type. */
    WORK_SOURCE_TYPE,

    /** The cwr excerpt type. */
    CWR_EXCERPT_TYPE,

    /** The cwr composite type. */
    CWR_COMPOSITE_TYPE,

    /** The agreement source type. */
    AGREEMENT_SOURCE_TYPE,

    /** The agreement type. */
    AGREEMENT_TYPE,

    /** The agreement work association. */
    AGREEMENT_WORK_ASSOCIATION,

    /** The dic. */
    DIC,

    /** The distribution method. */
    DISTRIBUTION_METHOD,

    /** The name type. */
    NAME_TYPE,

    /** The tariff param type. */
    TARIFF_PARAM_TYPE,

    ADDITIONAL_FIELD;

}
